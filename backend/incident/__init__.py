"""Incident console package (MochaTrade Flash-Crash Console).

New code for the incident console lives here, separate from the existing
`simulation/`, `liquidation/`, `cascade_model/` etc. packages, which are
imported by later handoffs rather than rewritten (PLAN.md §3.4).
"""
